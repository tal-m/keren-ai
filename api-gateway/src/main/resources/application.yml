server:
  port: 4003

# --- Actuator / Health ---
management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always

spring:
  cloud:
    gateway:
      routes:
        - id: auth-service-route
          uri: http://auth-service:4001
          predicates:
            - Path=/api/v1/auth/**
          filters:
            - name: RewritePath
              args:
                regexp: "/api/v1/auth/(?<segment>.*)"
                replacement: "/auth/public/${segment}"

        - id: admin-service-route
          uri: http://admin-service:4000
          predicates:
            - Path=/api/v1/admin/**
          filters:
            - name: RewritePath
              args:
                regexp: "/api/v1/admin/(?<segment>.*)"
                replacement: "/admin/${segment}"

      globalcors:
        add-to-simple-url-handler-mapping: true
        cors-configurations:
          '[/**]':
            allowed-origins: "${FRONTEND_URL:http://localhost:4173}"
            allowed-methods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowed-headers: "*"
            allow-credentials: true
